/**
 * Shared "navy pill" button treatment — the deep cobalt (#081549) rounded
 * pill with white text used for the task status-transition button and table
 * row View actions. Kept in one place so the two surfaces can't drift.
 */
export const MHD_PILL_BUTTON_CLASS =
  'inline-flex items-center gap-1.5 rounded-full bg-[#081549] font-medium text-white shadow-[inset_0_1px_0_rgba(255,255,255,0.18),inset_0_-1px_0_rgba(0,0,0,0.35),0_1px_3px_rgba(0,0,0,0.35)] transition-colors hover:bg-accent-hover';
