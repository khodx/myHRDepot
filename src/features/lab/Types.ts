// Lab & Sandbox feature — Platform Admin only (see mhdRouteAccess.ts '/lab').

/** A synthetic company created for QA, identified by the `[Sandbox]` name
 *  prefix rather than a dedicated column — see MhdLabTestDataSection for why. */
export const MHD_SANDBOX_COMPANY_PREFIX = '[Sandbox]';
