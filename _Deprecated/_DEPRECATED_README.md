# _Deprecated — Foundation-Codebase Config

Original config files with confirmed issues, preserved for reference.

## Files

| File | Replaced By | Issue Found |
|---|---|---|
| .env.example | ../.env.example | MED-003 — missing VITE_APP_URL and VITE_APP_VERSION variables |
| package.json | ../package.json | HIGH-002 — missing react-hook-form and @hookform/resolvers dependencies (used in components but not declared) |

The active, corrected files live at the parent directory.
